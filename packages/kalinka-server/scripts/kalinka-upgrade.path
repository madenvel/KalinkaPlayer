[Unit]
Description=Watch for kalinka upgrade requests
# No After=kalinka.service — see kalinka-restart.path.

[Path]
PathChanged=/run/kalinka/upgrade-request
Unit=kalinka-upgrade.service

[Install]
WantedBy=multi-user.target
